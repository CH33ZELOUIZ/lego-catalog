# Metadata strategy

A practical fallback chain for catalog apps:

1. direct page/API metadata when available
2. search-result title fallback
3. secondary source fallback
4. manual override file for stubborn entries

Never store denial pages, rate-limit pages, or error text as real item names.
